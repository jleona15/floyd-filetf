Compiles with the default make command, "make".

This program uses a graph class to hold an adjacency matrix implementation.

Which test case is ran depends on the TEST macro, brute-force for TEST = 0,
floyd-warshall for TEST = 1, dijkstra's algorithm otherwise.
